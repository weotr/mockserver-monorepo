exclude: ["vendor", "*.rb", "Gemfile*", "*.gemspec", "*.md", "*.pptx", "*.psd", "images/MockServerScenarios", "images/diagram-tools"]
include: [".well-known"]

# Site settings
title: MockServer
email: jamesdbloom@gmail.com
description: MockServer is an open-source HTTP(S) mock server, proxy, and recorder for testing. Mock any HTTP API, record and replay traffic, and connect AI assistants via the built-in MCP (Model Context Protocol) endpoint. Available as Docker, Maven, npm, Python, and Ruby.
keywords: mockserver, mock server, mock http server, http mock server, mock-server, mock rest service, mock server java, mock api, mock api server, json mock service, api mocking, api proxying, proxy, introspect http, proxy http, proxy https, proxy rest api, mcp server, model context protocol, ai coding assistant, claude code, cursor, windsurf, ai mock api, llm http testing, openapi mock
short_description: Mock HTTP/HTTPS APIs for testing, record and replay traffic, and integrate with AI coding assistants via MCP.
tagline: Mock any HTTP service. Record traffic. Now with MCP for AI assistants.
baseurl: "" # the subpath of your site, e.g. /blog/
url: "https://www.mock-server.com"
github_repo: https://github.com/mock-server/mockserver-monorepo
mockserver_version: 7.1.0
mockserver_api_version: 7.1.x
mockserver_snapshot_version: 7.1.1-SNAPSHOT

# Analytics — Google Analytics 4 (GA4)
# Measurement ID from GA4 admin → Data Streams → Web stream. Leave empty to disable analytics entirely.
ga4_measurement_id: G-20BB7EJG4E

# Social / brand assets
social_card: /images/social-card.png
twitter_handle: ""

# Slack community — the current Slack "shared invite link".
# The /slack.html page redirects visitors here, so external references can always
# point at /slack.html and never break. Leave empty to show the GitHub Discussions
# fallback instead of redirecting. (This comment lives only in the config — it is
# never rendered into any HTML.)
#
# ⚠️ EXPIRES — Slack shared-invite links are valid for only 30 days.
#   Current link: generated 2026-06-04  →  expires ~2026-07-04.
#   When it expires (the /slack.html redirect will start 404-ing at Slack), regenerate:
#     1. Slack → Invite people → "Share invite link" → Copy link.
#        (Ignore the email boxes — the link is open self-serve join and works on its own.)
#     2. Replace the slack_invite_url value below with the new link.
#     3. Update the "generated / expires" dates in this comment.
#     4. Rebuild and redeploy the site.
slack_invite_url: "https://join.slack.com/t/mock-server/shared_invite/zt-3zvvp22s3-krOtdL12EyE1s4jOptG8jQ"

sitemap:
    exclude:
        - "/css/main.scss"
        - "/error403.html"
        - "/google2b19680b073ab17b.html"

# Author
author:
  name:
    full: James D Bloom
    given: James
    family: Bloom
  image: https://blog.jamesdbloom.com/jamesdbloom.jpg
  gender: male
  blog: https://blog.jamesdbloom.com
  github: https://github.com/jamesdbloom

# Build settings
markdown: kramdown
sass:
    style: :compressed
    silence_deprecations:
        - import
        - global-builtin
        - color-functions
