[package]
name = "scenario-example"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
mockserver-client = { path = "../../../mockserver-client-rust" }
reqwest = { version = "0.12", features = ["blocking"] }
